document.addEventListener("DOMContentLoaded", () => {
    const board = document.getElementById("chess-board");
    // Fetch board state from server and render
});

def test_make_move():
    game = ChessGame()
    assert game.make_move("e2e4") == True
